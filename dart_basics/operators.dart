void main() {
  num i = 0;

  //i = i++; // i++/++i => i=i+1;

  // i = ++i; //suffix

  // print(i);

  // i = i + 1; // i++/++i

  // i = i + 2; // i+=2;

  i += 10; // i = i+10;
  i -= 20; 
  i *= 30;
  i /= 10;
}
